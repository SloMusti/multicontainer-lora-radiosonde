version: '2'
volumes: 
    resin-data:
services:
  lora:
    build: ./lora/
    privileged: true
    restart: always
    network_mode: host
  radiosonde:
    build: ./radiosonde/
    privileged: true
    restart: always
    network_mode: host
    expose:
      - "80"
    volumes: 
      - 'resin-data:/data'
